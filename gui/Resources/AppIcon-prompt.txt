Created with the built-in image-generation tool.

Create a finished premium macOS app icon for SparkServe, a Mac utility that controls AI model serving across two linked GPU server nodes. One square icon asset, icon alone, no mockup, no text or lettering, no logos. Match the polished dimensional macOS style of a red video-to-text icon: rounded-square tile, softly beveled edges, smooth sculpted white/silver symbols, gentle shadows, restrained materials, bold shapes legible at 32 pixels. For this icon use a deep emerald-green tile with lively lime-green highlights. Center two compact silver-white server blocks side by side, slightly overlapping in a balanced composition, each with only two simple vent slots; a bold bright lime-white lightning spark bridges them, clearly conveying two powerful linked compute nodes. Keep it elegant and very simple, not a rack diagram, not a scene, no decorative particles or tiny circuit details. Flat front view with just slight depth. Tile fills approximately 84 percent of the square canvas with generous transparent outer margins. Truly transparent background outside the rounded tile, subtle shadow only, no checkerboard or solid backdrop. Render one beautiful, finished icon.
